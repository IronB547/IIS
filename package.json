{
  "name": "iis-smartcity-frontend",
  "version": "1.0.0",
  "description": "A smart city project for IIS @ FIT VUT.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd frontend && yarn install && yarn build && cd ../backend && npm install",
    "run": "cd backend && npm run-script run"
  },
  "author": "Tomáš Dvořák, Juraj Dedič",
  "license": "ISC"
}
